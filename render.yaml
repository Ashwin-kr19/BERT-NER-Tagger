services:
  - type: web
    name: ner-tagger-api
    runtime: python
    plan: free
    autoDeploy: false
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT